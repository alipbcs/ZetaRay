set(APP_DIR "${ZETA_CORE_DIR}/App")
set(APP_SRC
	"${APP_DIR}/ZetaRay.h"
    "${APP_DIR}/App.h"
    "${APP_DIR}/Filesystem.h"
    "${APP_DIR}/Common.h"
    "${APP_DIR}/Log.h"
    "${APP_DIR}/Timer.h")
set(APP_SRC ${APP_SRC} PARENT_SCOPE)