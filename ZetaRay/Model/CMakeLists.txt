set(MODEL_DIR "${RENDERER_DIR}/Model")
set(MODEL_SRC
    "${MODEL_DIR}/glTF.cpp"
    "${MODEL_DIR}/glTF.h"
    "${MODEL_DIR}/glTFAsset.h"
    "${MODEL_DIR}/Mesh.cpp"
    "${MODEL_DIR}/Mesh.h")
set(MODEL_SRC ${MODEL_SRC} PARENT_SCOPE)